/* eslint-disable linebreak-style */
// eslint-disable-next-line linebreak-style

'use strict';

const { suma } = require('./operations');
const hello = () => 'Linter';

console.log(hello());

suma(2, 10);
